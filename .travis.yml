language: java
sudo: false
dist: trusty
jdk:
  - oraclejdk8

branches:
  only:
    - master

cache:
  directories:
  - $HOME/.gradle

script:
  - ./test.sh
  - ./gradlew test

after_success:
  - bash <(curl -s https://codecov.io/bash)
